# Required: OpenAI API key for extraction, embeddings, query rewriting, reranking
OPENAI_API_KEY=your-openai-api-key-here

# Optional: if set, all endpoints require Authorization: Bearer <token>
# MEMORY_AUTH_TOKEN=